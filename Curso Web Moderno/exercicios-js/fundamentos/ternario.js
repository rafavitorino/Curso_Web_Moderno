const resultado = nota => nota >=7 ? 'Aprovado' : 'Reprovado';
                                    //Verdaveiro    Falso

console.log(resultado(7.1))
console.log(resultado(6.7))


const namorando = pedido => {
    return pedido == 'sim' ? 'Namorando' : 'Solteiro';
}

console.log(namorando('sim'))

